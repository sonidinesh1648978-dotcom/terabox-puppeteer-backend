# Puppeteer Backend (Render Ready)

A simple Node.js backend using **Express + Puppeteer**, designed to be deployed easily on **Render** without configuration errors.

---

## 🚀 Features

- Headless browser automation using Puppeteer
- Express server with health-check endpoint
- Compatible with Render Free Tier
- Uses Node.js 18+
- Safe Chromium launch flags (`--no-sandbox`)

---

## 📁 Project Structure
